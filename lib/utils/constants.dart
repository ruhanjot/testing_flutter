import 'package:flutter/painting.dart';

// The main black/ dark grey color that will be used everywhere
const Color mainBlack = Color(0xFF222831);

// The accent grey color that will go on things such as the appbar
const Color accentGrey = Color(0xFF393E46);

// Highlight green color which will be used to add some color (maybe for an icon in a button)
const Color highlightGreen = Color(0xFF00ADB5);

// White-ish color for text and stuff
const Color mainWhite = Color(0xFFEEEEEE);

const double bodyFontSize = 18;

